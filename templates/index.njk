<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, minimum-scale=1, initial-scale=1">
    <title>{{ title }}</title>
    <meta name="description" content="{{ description }}">
    <script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.0.1/dist/alpine.js" defer></script>
    <link rel="stylesheet" href="{{ config.docsUrl }}/static/css/layout.css">
</head>
<body>
<script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.0.1/dist/alpine.js" defer></script>
{% include './includes/header.njk' %}
<div class="relative pt-16 pb-20 px-4 sm:px-6 lg:pt-24 lg:pb-28 lg:px-8">
  <div class="relative max-w-7xl mx-auto">
    <div class="text-center">
      <h2 class="text-3xl leading-9 tracking-tight font-extrabold text-gray-900 sm:text-4xl sm:leading-10">
        Baseline Blog
      </h2>
      <p class="mt-3 max-w-2xl mx-auto text-xl leading-7 text-gray-500 sm:mt-4">
        Welcome to our world 🤗
      </p>
    </div>
    <div class="mt-12 grid gap-5 max-w-lg mx-auto lg:grid-cols-3 lg:max-w-none">
    {% for item in posts %}
      <div class="flex flex-col rounded-lg shadow-lg overflow-hidden">
          <div class="flex-shrink-0">
            <img class="h-48 w-full object-cover" src="{{item.cover}}" alt="">
          </div>
          <div class="flex-1 bg-white p-6 flex flex-col justify-between">
            <div class="flex-1">
              <a href="{{ item.link }}" class="block">
                <h3 class="mt-2 text-xl leading-7 font-semibold text-gray-900">
                  {{item.title}}
                </h3>
                <p class="mt-3 text-base leading-6 text-gray-500">
                  {{item.excerpt | safe}}
                </p>
              </a>
            </div>
            <div class="mt-6 flex items-center">
              <div class="flex-shrink-0">
                <a href="#">
                  <img class="h-10 w-10 rounded-full" src="{{item.author.avatar}}" alt="">
                </a>
              </div>
              <div class="ml-3">
                <p class="text-sm leading-5 font-medium text-gray-900">
                  <a href="#" class="hover:underline">
                    {{item.author.fullName}}
                  </a>
                </p>
                <div class="flex text-sm leading-5 text-gray-500">
                  <time datetime="2020-03-16">
                    {{item.date}}
                  </time>
                  <span class="mx-1">
                    &middot;
                  </span>
                  <span>
                    {{item.readingTime}}
                  </span>
                </div>
              </div>
            </div>
          </div>
        </div>
    {% endfor %}
    </div>
  </div>
</div>

</body>
<script async defer data-domain="blog.baseline.dev" src="https://plausible.io/js/plausible.js"></script>
</html>
